"use client";

import { Button } from "./components/button";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center p-24">
      <Button onClick={() => {}}>Flow 1</Button>
      <Button onClick={() => {}}>Flow 2</Button>
    </main>
  )
}
